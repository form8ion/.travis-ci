branches:
  except:
    - /^v\d+\.\d+\.\d+(-(alpha|beta)\.\d+(@(alpha|beta))?)?$/

before_install:
  - curl ifconfig.co

deploy:
  - provider: script
    edge: true
    script: npx semantic-release
    on:
      all_branches: true
